# 天眼·艨艟战舰 (Vault App)

这是一个端到端加密云盘与本地保险箱的 Flutter 应用。

## 更新历史 (Changelog)

### 1.1.2 (2026-04-16)

#### ✨ Features
- 新增：主页数据概览模块，使用饼图（fl_chart）直观展示已加密与未加密文件大小比例，并自动换算 MB/GB。
- 新增：设置页加入"关于"页面，展示应用名称、当前版本号、包名等基本信息。
- 新增："关于"页面内置"更新日志"入口，采用手风琴（Accordion）模式折叠展示所有历史版本记录。
- 新增：加密任务进度面板，右上角动态图标入口，支持多根目录视图隔离及树状文件夹展示，包含极细进度条与精准统计。
- 变更：恢复 GitHub Actions `push` 与 `pull_request` 自动触发编译。开发过程中严格遵守"无指令不主动 push"规则。

---

### 1.1.1 (2026-04-16)

#### 🐛 Bug Fixes
- 修复：创建保险箱时部分重计算放到后台执行，降低卡死/ANR 风险。
- 修复：基准测试进度溢出问题，新增阶段与已处理 MB 文本展示，并支持多核并行加密。
- 优化：基准测试测试文件生成到所选保险箱目录并展示路径，便于确认生成正常。
- 优化：云盘列表交互——点击卡片进入预览页；新增独立编辑按钮（与删除并列）；创建/编辑时弹窗提醒安全存储等级；编辑页显示授权密码存储状态（不泄露密码）。

---

### 1.1.0 (2026-04-16)

#### ✨ Features
- 新增：加密保险箱（Vault）管理能力：创建/配置/列表展示/解锁进入保险箱。
- 新增：支持选择加密算法（AES-256-GCM、ChaCha20-Poly1305）与防暴力破解 KDF（PBKDF2、Argon2id、Scrypt），并可调整各自参数。
- 新增：保险箱根目录写入明文 `vault_config.json`，包含算法/KDF/参数/是否加密文件名与校验密文块。
- 新增：配置页右上角性能测试（Benchmark）：生成并加密 500MB 临时数据，展示平均加密速度（MB/s），测试结束自动清理临时文件。
- 优化：全局赛博朋克主题升级为高对比度霓虹配色，并对加密相关页面进行适配。

---

### 1.0.2 (2026-04-16)

#### ✨ Features
- 新增：加密导航栏加入系统文件选择器，支持选择文件或文件夹，为后续加密功能提供数据来源。
- 新增：动态请求存储权限，完美兼容 Android 11+（所有文件访问权限）和低版本 Android 存储权限，且仅在点击时请求，体验更优。
- 新增：适配应用两种主题（默认主题、赛博朋克）的优美 UI 界面。

---

### 1.0.1 (2026-04-16)

#### ✨ Features
- 新增：云盘页引入完整的 WebDAV 配置管理功能（增、删、改、查）。
- 新增：根据 Android 设备硬件级密钥存储能力自动检测安全等级（Level 1 / Level 2），并提供图形化警告提示。
- 新增：配置本地持久化能力，非敏感字段存储至 JSON，密码凭证保存于 Flutter Secure Storage 安全容器。

#### 🐛 Bug Fixes
- 修复：解决主题切换时，底部导航栏中“设置”页面的主题选项按钮状态未即时刷新的问题。
- 修复：解决 Android 生产构建打包时，无法正确读取 GitHub Actions Secrets（`KEY_STORE_BASE64`, `KEY_ALIAS`, `KEY_PASSWORD`, `STORE_PASSWORD`）并执行生产签名的问题，确保更新后的 APK 签名一致以支持覆盖安装。

#### 🛠 Build & CI
- 变更：优化 GitHub Actions 打包工作流，强制拦截无签名构建流程。

---

### 1.0.0
- 初始版本发布，搭建基础 Flutter 项目框架及基础的错误拦截体系。
