在编译apk时，需要自己查看报错并修复。在编译过程中必须在浏览器（或通过gh cli等工具）等待，直到确定新发布的版本（GitHub Release）中包含APK安装包为止。

【项目提交与编译规则】
1. 提交代码至项目仓库时，必须提交所有的配置、答疑文件和上下文文档，以确保用户在使用新副本时能够获取到完整配置和历史思路。
2. 绝对不能将含有用户密钥的文件（如 .github_token 或 keystore 等敏感文件）提交到仓库。务必确保它们存在于 .gitignore 中，并在 commit 前进行二次检查。
3. 只有当用户明确发送包含“编译”字样的指令时，才允许触发编译发布 APK 的流程。日常的 Bug 修复或功能迭代请只提交代码至 GitHub，禁止擅自启动打包流程。
