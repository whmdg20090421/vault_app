- [x] 安全设置页面入口存在，且进入后支持配置临时状态与"确认"保存功能。
- [x] 安全设置页面中，修改配置后未保存退出时，会弹出确认提示框；确认退出后配置恢复原状。
- [x] 安全设置页面中，切换 Root 模式时会进行权限检测/申请，授权失败时保存按钮置灰。
- [x] 拥有 Root 权限后，支持选择"默认"或"始终"行为。
- [x] 首页显示本地加密、云端加密、差异文件数量，且提供刷新按钮。
- [x] 差异计算逻辑能够正确对比文件结构和哈希值，得出差异文件数。
- [x] 设置中包含"启动时自动刷新差异"的选项，开启后 App 启动时能自动执行差异计算。
- [x] 本地加密操作完成后，能够正确更新 `local_index.json`（含结构与哈希值）。
- [x] 云端上传流程遵循 `remote_index_cache.json` 规范（下载/创建、更新、上传覆盖）。
- [x] 每次准备上传前，能够进行 `remote_index.json` 的一致性校验，哈希不一致时弹出同步提示框。
