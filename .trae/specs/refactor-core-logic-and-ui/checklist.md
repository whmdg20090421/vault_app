- [x] `pubspec.yaml` 成功添加 `dio`, `path_provider`, `image_picker`, `xml` 等依赖并获取成功。
- [x] 原云盘主页的同步新建入口已被移除，同步操作完全绑定至 `WebDAVDashboardPage`。
- [x] 参考 `dom111/webdav-js`，成功使用 Dio 发送 `PROPFIND` 并解析 XML 以获取目录状态。
- [x] 成功基于差异比对（Diffing）使用原生网络库实现了 HTTP PUT、GET、DELETE 等双向传输逻辑。
- [x] `Failed host lookup` 等 WebDAV 连接异常被修复，请求 Headers（如 Depth）设置正确。
- [x] 捕获的网络异常已成功写入 Android 外部私有目录日志文件。
- [x] 加密/解密过程已成功放入独立 `Isolate`，大文件处理时 UI 保持流畅（无假死）。
- [x] 加密任务 UI 呈现递归目录结构，支持点击深入子目录。
- [x] 目录进度能够根据子节点真实状态冒泡更新（显示总大小、已处理大小、百分比）。
- [x] 主页数据概览进度条呈现科技感（渐变、阴影），并伴随平滑生长动画。
- [x] 主页概览文本格式严格遵守 `🟩 已加密: XX MB (XX%)` 的精确显示。
- [x] “主题与背景设置”二级页面已建立，包含开关和图片选择功能。
- [x] 自定义背景图能被复制到内部私有目录并成功设为全局背景。
- [x] 两个透明度滑块能够独立且正确地控制 UI 组件透明度与背景遮罩透明度。
- [x] `.trae/rules.md` 中已包含关于“发版强制规范”的新规则。
- [x] CHANGELOG.md 或类似日志文件中已补齐 1.2.0、1.2.1、1.2.2 版本的记录。
