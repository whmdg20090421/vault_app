+ [x] 创建并保存时不会卡死/ANR：在 PBKDF2 高迭代（如 600000）下仍可响应
+ [x] 创建并保存时 UI 有明确加载态（按钮禁用 + 进度指示），成功/失败均会恢复
+ [x] Benchmark 进度条与百分比永远不超过 100%，完成时为 100%
+ [x] Benchmark 在多核配置下会启用多个 worker（多 isolate 并行），单核不再长期独占
+ [x] 性能设置页可打开，显示“(当前可使用核心/总核心)”并提供 Slider + 输入框联动
+ [x] 核心数量限制生效：1 <= cores <= (maxCores - 1)，并能持久化到本地
+ [x] Benchmark 使用性能设置中的核心数量配置，并在测试结束后清理临时资源

