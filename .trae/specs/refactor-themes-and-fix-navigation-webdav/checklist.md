- [x] 赛博朋克主题已改为圆角与合适的渐变彩色背景。
- [x] 纯黑主题已添加，风格极简无发光效果。
- [x] 全局自定义图片背景功能已在“关于”或设置页面实现（包含开关、图片选择、图片/UI透明度调节）。
- [x] 选中的背景图片已缓存到本地数据目录，开关状态和透明度已持久化。
- [x] 开启自定义背景时，应用的根背景会被替换为所选图片，且子界面背景呈现设定的 UI 透明度。
- [x] `vault_explorer_page.dart` 实现了基于目录深度的逐级返回拦截逻辑。
- [x] 程序中所有数据模块（统计、列表等）均已接入真实数据源，排除了所有硬编码模拟数据。
- [x] 数据展示模块实现了缓存机制，且空数据时正确显示“暂无数据”。
- [x] `home_page.dart` 数据概览已重构为线性进度条样式，且宽度满铺。
- [x] 文件大小已正确格式化为 KB/MB/GB。
- [x] WebDAV 已配置忽略证书校验，且 Android 网络配置（明文传输）已就绪。
- [x] 成功使用 Dart 原生网络库复刻 WebDAV 核心逻辑（`PROPFIND`、`DELETE`等），实现真实目录浏览、XML解析模型转换、删除及状态刷新。
- [x] UI 文件列表实现了云端与本地文件比对，并显示“已同步/未同步”状态。
- [ ] 已完成全局代码、逻辑与 UI 的自检。
- [ ] 代码成功推送到 GitHub 仓库并自动修复 CI 错误。
- [ ] v1.2.2 正式版成功发布，Release 包含正确的单版本日志。
