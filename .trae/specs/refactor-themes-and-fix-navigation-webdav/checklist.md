- [ ] 赛博朋克主题已改为圆角与合适的渐变彩色背景。
- [ ] 纯黑主题已添加，风格极简无发光效果。
- [ ] 全局自定义图片背景功能已在“关于”或设置页面实现（包含开关、图片选择、图片/UI透明度调节）。
- [ ] 选中的背景图片已缓存到本地数据目录，开关状态和透明度已持久化。
- [ ] 开启自定义背景时，应用的根背景会被替换为所选图片，且子界面背景呈现设定的 UI 透明度。
- [ ] `vault_explorer_page.dart` 实现了基于目录深度的逐级返回拦截逻辑。
- [ ] 程序中所有数据模块（统计、列表等）均已接入真实数据源，排除了所有硬编码模拟数据。
- [ ] 数据展示模块实现了缓存机制，且空数据时正确显示“暂无数据”。
- [ ] `home_page.dart` 数据概览已重构为线性进度条样式，且宽度满铺。
- [ ] 文件大小已正确格式化为 KB/MB/GB。
- [ ] WebDAV 已配置忽略证书校验，且 Android 网络配置（明文传输）已就绪。
