# Tasks

- [ ] Task 1: 修复首页刷新云端文件和差异文件数量统计
  - [ ] SubTask 1.1: 在首页或相关状态管理中，添加或修复获取云端指定同步文件夹内文件总数的逻辑（如果是加密文件夹，获取加密文件的数量）。
  - [ ] SubTask 1.2: 添加或修复差异文件计算逻辑，通过对比云端文件列表和本地文件（大小/索引）计算出真正需要同步的文件数量。
- [ ] Task 2: 修复云端文件浏览器中“未同步”状态的显示
  - [ ] SubTask 2.1: 在云端文件浏览器 (`WebDavBrowserPage` 等) 中，定位文件状态（“已同步”/“未同步”）的判断逻辑。
  - [ ] SubTask 2.2: 修改判断逻辑，使其与 `SyncEngine` 一致（例如通过文件大小而不是仅通过时间戳判断是否一致），确保已同步的文件正确显示为已同步。
- [ ] Task 3: 修复点击“开始同步”后按钮无限转圈的问题
  - [ ] SubTask 3.1: 在 `SyncSettingsDialog` 或相关页面中，找到“开始同步”的 `onPressed` 逻辑。
  - [ ] SubTask 3.2: 调整逻辑为：计算差异文件 -> 推送任务到进度管理器 -> 结束加载状态（`setState(() => isLoading = false)`）并关闭弹窗或恢复按钮。
- [ ] Task 4: 在同步进度中添加上传/下载速度和预计剩余时间
  - [ ] SubTask 4.1: 在 `SyncTask` 数据模型中增加速度和剩余时间的字段。
  - [ ] SubTask 4.2: 在 `CloudDriveProgressManager` 或相关进度回调中，计算每秒传输的字节数（速度）以及基于剩余字节的剩余时间。
  - [ ] SubTask 4.3: 在同步进度详情界面中展示计算出的速度和剩余时间。
