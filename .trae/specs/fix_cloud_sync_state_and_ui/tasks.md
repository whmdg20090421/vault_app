# Tasks

- [x] Task 1: 完善 WebDAV 客户端和数据模型，支持 LOCK/UNLOCK 以及解析 PROPFIND 属性
  - [x] SubTask 1.1: 在 `WebDAVClient` 中添加 `lock(path)` 和 `unlock(path, token)` 方法。
  - [x] SubTask 1.2: 在解析 `PROPFIND` 返回的 XML 数据时，确保正确提取 `getetag`, `getcontentlength`, `getlastmodified` 等属性到相关的数据模型中。
- [x] Task 2: 重构同步引擎中的同一性判断逻辑
  - [x] SubTask 2.1: 在 `SyncEngine` 或相关文件中，修改比较逻辑：使用 `PROPFIND` 获取的文件参数（如 ETag、大小）替代原有的不可靠的判断逻辑，以准确判断云端文件和本地文件是否为同一文件。
- [x] Task 3: 在同步引擎的上传/下载/修改逻辑中加入并发控制
  - [x] SubTask 3.1: 在文件上传、下载等操作之前调用 `LOCK`。
  - [x] SubTask 3.2: 确保在操作完成后（无论成功还是异常），都能调用 `UNLOCK` 释放云端文件。
- [x] Task 4: 修复首页刷新和云端文件浏览器的统计与状态显示
  - [x] SubTask 4.1: 修改首页获取云端文件数量的逻辑（计算云端加密文件夹内的文件总数）。
  - [x] SubTask 4.2: 修改首页获取差异文件数量的逻辑，使其基于新的 `PROPFIND` 同一性判断进行统计，而不仅是简单做减法。
  - [x] SubTask 4.3: 在云端文件浏览器中，确保每个文件的“未同步”/“已同步”状态能够使用新的同一性判断逻辑，准确显示。
- [x] Task 5: 修复点击“开始同步”后按钮无限转圈的问题
  - [x] SubTask 5.1: 在 `SyncSettingsDialog` 中，调整点击“开始同步”按钮的逻辑。
  - [x] SubTask 5.2: 逻辑流程应为：执行差异计算 -> 将需要同步的任务推送到 `CloudDriveProgressManager` -> 结束加载状态并恢复按钮。
- [x] Task 6: 在同步进度中添加上传/下载速度和预计剩余时间
  - [x] SubTask 6.1: 在 `SyncTask` 中添加速度、预计剩余时间等相关字段。
  - [x] SubTask 6.2: 在上传和下载进度的回调中，根据已传输字节数和流逝的时间，动态计算每秒传输速度（MB/s 或 KB/s）以及剩余时间。
  - [x] SubTask 6.3: 在进度详情界面展示这些信息。
