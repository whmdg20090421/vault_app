- [x] WebDAV 客户端中已实现 `LOCK` 与 `UNLOCK`，并且能够在网络请求时正确解析 `PROPFIND` 中的属性。
- [x] 首页点击刷新时，能够基于 `PROPFIND` 准确统计并显示云端文件数量及真实差异文件数量。
- [x] 在云端文件浏览器中查看已完全同步的文件时，文件状态基于新的判断逻辑（而非简单的不可靠参数）正确识别并显示“已同步”。
- [x] 在上传、下载或修改云端文件操作期间，云端文件会被锁定 (`LOCK`)，并且在操作结束后锁定被正确释放 (`UNLOCK`)。
- [x] 在同步设置中点击“开始同步”后，系统在计算差异文件完成后会自动恢复按钮状态，并成功将任务推送到同步进度中，不再无限转圈。
- [x] 在同步任务进度界面中，能够正常显示当前任务的上传或下载速度。
- [x] 在同步任务进度界面中，能够正常显示当前任务的预计剩余时间。
