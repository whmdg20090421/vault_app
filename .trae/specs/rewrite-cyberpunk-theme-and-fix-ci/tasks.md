# Tasks
- [ ] Task 1: 彻底重写赛博朋克主题。
  - [ ] SubTask 1.1: 扫描并定位现有的赛博朋克主题代码，覆盖全局背景色、卡片背景色及各级强调色（如 `#0F1418`, `#00E5FF` 等）。
  - [ ] SubTask 1.2: 更改字体排版，标题设为无衬线粗体大写，核心数据和系统信息强制使用等宽字体 (Monospace)。
  - [ ] SubTask 1.3: 调整组件样式规则（发光效果、单边粗边框、准星折角线、直角按钮悬浮发光、输入框、实心方块导航栏、极细进度条等），严格禁用圆角。
- [ ] Task 2: 全局扫描修复与 Android 版本号防降级。
  - [ ] SubTask 2.1: 对整个代码库进行自检，修复任何明显的语法或逻辑 Bug。
  - [ ] SubTask 2.2: 定位 Android 构建配置文件（如 `android/app/build.gradle`），重写版本号控制逻辑，确保每次打包时 `versionCode` 能准确读取本地记录并严格 +1。
  - [ ] SubTask 2.3: 更新当前版本号 `versionName` 为 `1.2.0`。
- [ ] Task 3: 自动化提交、工作流构建与全自动纠错。
  - [ ] SubTask 3.1: 调用 Git 命令提交阶段一和阶段二的所有变更，并 Push 到远端仓库。
  - [ ] SubTask 3.2: 触发或等待远端工作流开始编译 APK，并密切监控编译终端或工作流的日志。
  - [ ] SubTask 3.3: 若遇到编译失败，自动读取报错日志，定位并修复代码或依赖问题，随后再次提交触发编译，直至成功构建。
  - [ ] SubTask 3.4: 编译成功后，将此版本标记并正式发布为 `1.2.0` 版本。
