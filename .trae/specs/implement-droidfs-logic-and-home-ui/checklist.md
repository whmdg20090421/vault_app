- [ ] 目录映射逻辑（加密路径 ↔ 明文路径）成功复刻 DroidFS 的 1:1 映射机制
- [ ] 文件名通过确定性加密算法加密，同一路径下名称固定的文件加密结果一致
- [ ] 文件头部包含随机生成的 16-byte File ID，并基于该 ID 派生不同分块的 Nonce
- [ ] 文件加密解密过程成功引入 Dart Isolate / 多线程处理机制以提升大文件性能
- [ ] 主页布局成功重构为左右各占一半，左侧为正方形概览卡片，右侧留空
- [ ] 主页概览图表数据来源于 `StatsService` 的真实扫描结果，而非硬编码
- [ ] 统计扫描结果持久化保存于 `SharedPreferences`（或 JSON），启动时只读缓存不全盘扫描
- [ ] 当触发指定事件（加解密、WebDAV 同步、手动刷新）时能够重新扫描更新缓存
- [ ] 主页图表文字与图例能够根据总体积自动换算（<1GB 显示 MB，>=1GB 显示 GB）
