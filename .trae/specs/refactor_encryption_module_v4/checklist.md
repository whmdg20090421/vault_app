- [x] **一、文件导入与路径获取**
  - [x] 1. 点击「导入加密文件夹」或「导入文件」可唤起系统原生选择界面并获取绝对路径。
- [x] **二、加密文件结构树定义与构建**
  - [x] 2. 任务根节点存储在全局任务列表中，支持JSON格式持久化至私有数据目录。
  - [x] 3. 文件夹节点包含 `taskId`, `name`, `type=folder`, `isPaused`, `children`。
  - [x] 4. 文件节点包含 `name`, `type=file`, `size` (自动换算1~1000保留1位小数), `rawSize`, `status` (5种状态), `isPaused`。
  - [x] 5. 构建时正确递归遍历，同步读取原始字节并换算 `size`，默认 `isPaused=false` 且文件状态为 `pending_waiting`。
  - [x] 6. 新任务创建后自动触发全局刷新并启动动态刷新机制。
- [x] **三、多线程加密执行**
  - [x] 7. 按设置的线程数并行处理 `status=pending_waiting` 且 `isPaused=false` 的文件节点。
  - [x] 8. 中断后重启可自动将中断任务重置为 `pending_waiting`，并执行完整重新加密。
  - [x] 9. 文件缺失/损坏时状态设为 `error`，UI标记异常且不影响其他文件。
- [x] **四、加密状态更新与进度恢复**
  - [x] 10. 开始加密时更新为 `encrypting`，完成时更新为 `completed`。
  - [x] 11. 重启时基于本地存储恢复任务状态与进度。
  - [x] 12. 重启时自动扫描文件，将不存在的文件设为 `error`。
- [ ] **五、主页导航栏加密信息总览**
  - [ ] 13. 主页顶部显示四色进度条图标，点击显示 `已加密大小 / 总大小` 数值提示。
  - [ ] 14. 绿色/黄色/红色/灰色进度条占比计算正确，且 <1% 的部分强制显示为 1 像素。
- [ ] **六、层级化加密进度详情查看**
  - [ ] 15. 点击「查看同步进度」滑出半屏模态框，展示所有正在进行的加密任务（同级关系），支持无限嵌套展开/折叠。
  - [ ] 16. 条目三行布局：左侧暂停/继续按钮、右侧名称（异常带红叹号）；单条极细四色进度条（遵循1像素强制规则）；左下百分比，右下数量统计。
  - [ ] 17. 暂停/继续按钮可级联控制子节点，状态正确流转（`encrypting` <-> `pending_paused` <-> `pending_waiting`）。
  - [ ] 18. 长按弹出「移除加密」菜单（异常含「标记为已修复」），支持二次确认。
  - [ ] 19. 移除操作仅删除该节点及子节点，终止对应线程并更新全局数据。
- [ ] **七、动态进度刷新机制**
  - [ ] 20. 后台静默状态：每 5~10 秒随机刷新，仅更新内存并持久化，不重绘UI。
  - [ ] 21. 前台显示状态：打开界面立即全量刷新，随后每 0.5~1 秒高频刷新（更新内存与UI）。
  - [ ] 22. 界面打开与关闭时能无缝切换高低频模式。
  - [ ] 23. 所有任务完成时执行最终全量刷新，缓存至本地，停止监听；新任务创建时重新启动监听。
- [x] **八、任务完成与自动归档**
  - [x] 24. 根节点下所有文件均 `completed` 时判定任务完成。
  - [x] 25. 完成后执行存在性检查，全存在移入历史记录，缺失则保留在进行中列表并将缺失文件设为 `error`。
  - [ ] 26. 历史记录列表隐藏暂停与进度条，显示完成时间，支持长按删除历史记录（不删文件与索引）。
- [x] **九、加密完成后处理**
  - [x] 27. 单个文件完成后，计算密文哈希，关联 `taskId + 相对路径` 写入本地哈希索引库。