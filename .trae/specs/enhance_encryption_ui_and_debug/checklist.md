- [x] 加密进度条的百分比旁能显示正确的、带缓存作用的实时加密速度（KB/s 或 MB/s）。
- [x] 进度条右侧的加密大小文字间距缩小。
- [x] 进度条右侧能显示基于实时速度计算得出的预计剩余时间（分和秒格式，如 `(01:30)`）。
- [x] 在设置页的“关于”选项上长按5秒后，能弹出进入开发者模式的警告对话框。
- [x] 确认进入开发者模式后，加密进度条下方会出现第3行。
- [x] 开发者模式的第3行能详细显示当前使用的加密算法、底层调用情况、底层加密库情况、元数据、线程加密负载等。
- [x] 上述所有 UI 的出现、消失或状态变化（如进度条增长、弹窗、面板展开）具有丝滑Q弹的动画效果。
- [x] 已完成批量检查：底层加密接口和 UI 线程通信不会导致崩溃，多线程和状态更新无性能问题或内存泄漏。
