- [x] 应用的“同一性检查”逻辑已被审查并书面解释，不再受上传时间影响（依赖大小和 ETag）。
- [x] 所有的应用设置（主题、背景图片、加密配置等）被持久化到了应用的内部私有数据目录的一个 JSON 文件中。
- [x] 每次启动应用时，所有的设置从该 JSON 文件中被成功读取并生效。
- [x] 每次在设置页面修改了任何参数后，该 JSON 文件的内容都会被同步更新。
- [x] 重启应用后，云盘同步任务列表能够正确加载未完成的任务。
- [x] 未完成的同步任务在点击“恢复/继续”时，能够通过重新获取云盘凭据继续同步，进度条不再卡死。
- [x] 同步完成的任务会被标记，并从正在进行的同步列表中移除，同时自动移入同步历史记录的 JSON 数据中。
- [x] 版本已升级，且修改内容已被提交并推送到 GitHub 触发自动编译。
