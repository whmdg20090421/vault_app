- [x] 同步设置中，点击“同步模式”下拉框时，弹出的菜单具有不透明背景色，不再与下方内容重叠。
- [x] 在“本地同步文件夹”中，可以点击进入某个已加密的文件夹并选择其子目录。
- [x] 在“云端同步文件夹”中，可以浏览 WebDAV 根目录并选择具体的远程子目录。
- [x] 执行云端同步时，日志不再报出 `DioException [bad response]: 401 Unauthorized` 错误，鉴权能成功通过。
- [x] （加密验证）通过代码审计/测试，确认向加密文件夹导入的新文件，其最终落地于磁盘的字节流必定为不可直接读取的密文格式（经由 ChunkCrypto 加密），修复了直接透传或空运行的 Bug。
