- [x] 底层加密库（cryptography）验证通过，无任何崩溃或仅占位的情况。
- [x] VaultExplorerPage 中长按文件/文件夹能够正确显示“重命名”菜单项。
- [x] 点击“重命名”可以弹出对话框输入新名称，并在确认后正确映射底层路径完成重命名并刷新界面。
- [x] VaultExplorerPage 中点击文件夹会弹出“是否打开文件夹 {文件夹名称}?”的确认弹窗。
- [x] 确认打开后，能够通过 `_currentPath`（明文路径）顺利找到真实的底层加密目录并列出文件。
- [x] Agent 独立检查完成，证明加密层到应用层的解密映射完全正确，参数传递与兼容性无问题。