- [x] GitHub Actions 工作流已成功配置 Flutter Pub 和 Gradle 缓存，且明确排除了版本控制文件。
- [x] 工作流文件支持 `workflow_dispatch` 手动输入缓存清理参数。
- [x] 自动检查并确保 `AndroidManifest.xml` 包含必要的网络权限。
- [x] WebDAV 网络请求已捕获底层异常，并转换为友好的中文 UI 提示。
- [x] 主页数据概览图表的 UI 越界问题已修复，且展示真实数据。
- [x] 加密任务选择文件夹后，UI 立即解阻塞，无多余的假加载。
- [x] 加密任务的真实 IO 工作已转移至后台 Isolate 执行，不阻塞主线程。
- [x] 加密任务能实时在进度面板中展示其真实的后台进度。
- [ ] 全局自检完成，修复了已知的内存泄漏和不安全的 `setState`。
- [ ] 终端本地执行 `flutter build apk --release` 成功，错误自愈循环未超过 6 次。
- [ ] 代码已成功 Commit、推送，并发布为正式版 `1.2.1`。