- [ ] GitHub Actions 工作流已成功配置 Flutter Pub 和 Gradle 缓存。
- [ ] `version.properties` 等版本控制文件被明确排除在缓存范围之外。
- [ ] 工作流文件支持 `workflow_dispatch` 手动输入缓存清理参数。
- [ ] `AndroidManifest.xml` 包含必要的网络权限。
- [ ] WebDAV DNS 解析失败或网络断开时，应用会给出友好的错误提示。
- [ ] 主页数据概览图表的 UI 越界问题已修复，图表完美适应父级卡片。
- [ ] 加密任务选择文件夹后，UI 立即解阻塞。
- [ ] 加密任务的 IO 工作已转移至后台 Isolate 执行。
- [ ] 加密任务能实时在进度面板中显示（初始 0%）。
- [ ] 全局自检完成，修复了已知的内存泄漏（如未调用的 `dispose`）和不安全的 `setState`。
- [ ] 终端本地执行 `flutter build apk --release` 成功，错误自愈循环未超过 6 次。
- [ ] 代码已成功 Commit、推送，并发布为版本 `1.2.1`。