- [x] 验证基础架构：已实现完整的 WebDAV 文件浏览器 UI（包含目录列表、面包屑导航、上传/新建/重命名/删除交互入口）。
- [x] 验证 GitHub Actions 工作流已被严格限制为仅包含 `workflow_dispatch` 手动触发配置。
- [x] 验证加密算法统一采用相同的对称加密方案（如 AES-256-GCM），且文件名与文件内容共享相同的用户密钥。
- [x] 验证 Base64(Url) 仅用作密文等二进制数据的传输编码，未直接充当加密手段。
- [x] 验证系统能够正确写入加密标识符（Marker），并根据该标识符实现子目录及文件的加密域状态向下递归继承。
- [x] 验证浏览（Browse）功能：成功拉取 WebDAV 目录，并在内存中将加密的文件名实时解密、还原为明文展示。
- [x] 验证改名（Rename）功能：对明文新名称进行加密编码，成功在云端执行重命名操作。
- [x] 验证删除（Delete）功能：可以准确地在云端删除对应的加密文件或加密目录。
- [x] 验证上传（Upload）功能：本地文件按分块模式被 AES-256-GCM 加密并上传，云端仅存储密文。
- [x] 验证打开（Open）与读取功能：系统通过按需 Range 请求分块下载大文件（如视频），并在内存中实时进行流式解密（Stream Decryption），不卡顿且明文数据全程不写入本地磁盘。