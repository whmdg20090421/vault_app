- [x] 文件导入逻辑：能够接收用户选择的绝对路径，正确解析并构建出完整的、包含唯一 `taskId`、文件夹与文件层级的 `EncryptionNode` 树结构。
- [x] 字段完备性：文件节点正确包含并自动换算了 `size` (1~1000间，B~TB)，且正确初始化 `status` 为 `pending_waiting` 及 `isPaused` 为 `false`。
- [x] 树结构持久化：全局加密任务列表成功序列化为 JSON，并存入应用内部私有数据目录，能在应用重启时正确恢复树结构。
- [x] 多线程加密调度：能根据配置的并发数，并行处理队列中 `status=pending_waiting` 且 `isPaused=false` 的文件，并在开始时置状态为 `encrypting`。
- [x] 加密与哈希后处理：加密完成后文件状态转为 `completed`，且成功计算哈希值，并将 (taskId+相对路径) 关联的哈希值写入本地哈希索引库 (`local_index.json`)。
- [ ] 异常中断恢复：测试因强制关闭引发中断后，应用重启能将中断文件状态重置为 `pending_waiting`，并自动对原路径丢失的文件标记为 `error` 且能在 UI 显示错误提示。
- [ ] 动态刷新机制 (前台)：当有进度 UI 处于打开状态时，进度监听线程切换至 0.5~1秒随机一次的高频刷新，UI与内存均同步更新。
- [ ] 动态刷新机制 (后台)：当关闭所有进度 UI 后，进度刷新切换至 5~10秒随机一次的低频静默刷新，仅持久化进度数据而不触发UI重绘。
- [ ] 导航栏进度 UI：主页顶部成功显示小型四色进度条（绿/黄/红/灰），计算逻辑准确反映各状态的 `rawSize` 占比，且点击能显示“已加密大小/总大小”提示。
- [ ] 进度条 1% 强制可视化：所有进度条（导航栏和详情列表），当某一颜色段有数据但占比不足 1% 时，强制保持最小 1 像素的显示长度。
- [ ] 模态框列表结构：成功从主界面滑出半屏模态框，同级展示所有任务根节点，并支持无限制的多级嵌套展开与折叠。
- [ ] 模态框三行布局：进度详情条目严格遵循三行布局（行1:暂停/继续按钮及名称，行2:细进度条，行3:总体百分比与 1/1 或已加密/总文件数统计）。
- [ ] 条目暂停与继续：点击暂停按钮能中止该节点及其子节点所有运行线程，设置 `isPaused=true` 并将状态转为 `pending_paused`；点击继续能重置为 `false`，状态恢复为 `pending_waiting`。父节点暂停行为覆盖子节点。
- [ ] 长按移除操作：长按节点能唤出操作菜单，二次确认后成功释放资源，将当前及子节点彻底移出全局任务树，且绝不影响同级或父节点的加密状态。
- [ ] 长按修复操作：对异常节点长按选择“标记为已修复”后，能将其 `error` 状态成功重置为 `pending_waiting` 并重新入列。
- [x] 自动归档：任务下所有文件完成（皆为 `completed`）时，自动执行原目录检查，全存在则移至历史记录；有缺失则标记 `error` 并保留在活跃列表。
- [ ] 历史记录管理：历史记录列表能够展示归档任务（隐藏进度与控制按钮），且长按能执行二次确认的仅清除进度数据的删除操作。
